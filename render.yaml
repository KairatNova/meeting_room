databases:
  - name: meeting-room-db
    plan: free

services:
  - type: web
    name: meeting-room-backend
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: meeting-room-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "false"
      # IMPORTANT: update after frontend deploy if you rename the service.
      - key: CORS_ORIGINS
        value: https://meeting-room-frontend.onrender.com
      # Persist uploads (room images) on a Render disk.
      - key: UPLOAD_DIR
        value: /var/data/uploads
    disk:
      name: uploads
      mountPath: /var/data
      sizeGB: 1

  - type: static
    name: meeting-room-frontend
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      # IMPORTANT: update if you rename the backend service.
      - key: VITE_API_URL
        value: https://meeting-room-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

